<% content_for :structured_data do %>
  <%= render Site3::StructuredDataComponent.new(breadcrumbs: breadcrumbs) %>
<% end %>

<div class="mx-auto max-w-[80rem] px-3 sm:px-5 pb-8">
  <%= render Site3::BreadcrumbsComponent.new(items: breadcrumbs) %>

  <header class="mt-5 mb-10 flex flex-wrap items-end justify-between gap-6">
    <div class="max-w-2xl">
      <span class="inline-flex items-center h-7 px-3 rounded-full bg-s3-mint-soft
                   text-s3-mint text-[0.8125rem] font-medium mb-4">
        <%= t("site3.offers.count", count: @pagy.count) %>
      </span>
      <h1 class="font-s3-display text-5xl sm:text-6xl leading-[0.98] tracking-[-0.025em] text-s3-ink">
        <%= t("site3.offers.title") %>
      </h1>
      <p class="mt-3 text-[1.0625rem] text-s3-ink-2"><%= t("site3.offers.lead") %></p>
    </div>
  </header>

  <% if @vehicles.any? %>
    <div class="grid gap-3 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4"
         data-controller="site3-reveal">
      <% @vehicles.each_with_index do |vehicle, index| %>
        <div data-site3-reveal-target="item">
          <%= render Site3::VehicleCardComponent.new(
                vehicle: vehicle,
                loading: index < 4 ? "eager" : "lazy",
                priority: index.zero?) %>
        </div>
      <% end %>
    </div>

    <%= render UI::PaginationComponent.new(@pagy, theme: :site3) %>
  <% else %>
    <%= render Site3::EmptyStateComponent.new(
          title: t("site3.offers.empty_title"),
          description: t("site3.offers.empty_lead")) do |empty| %>
      <% empty.with_action do %>
        <%= link_to t("site3.cta.explore"), site3_vehicles_path,
              class: "inline-flex items-center h-11 px-6 rounded-full bg-s3-accent text-white
                      text-sm font-medium" %>
      <% end %>
    <% end %>
  <% end %>
</div>
