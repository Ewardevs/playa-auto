<div class="mx-auto max-w-[88rem] px-3 sm:px-5 mb-6" role="status" aria-live="polite">
  <% messages.each do |tone, message| %>
    <div data-controller="dismiss" data-dismiss-delay-value="8000"
         class="s4-card flex items-center gap-4 px-5 py-4">
      <span class="grid place-items-center size-9 rounded-full shrink-0 <%= tone[:chip] %>">
        <%= render UI::IconComponent.new(tone[:icon], class: "size-4") %>
      </span>

      <p class="flex-1 text-[0.9375rem] text-s4-ink"><%= message %></p>

      <button type="button" data-action="dismiss#close"
              class="shrink-0 grid place-items-center size-8 rounded-full text-s4-ink-3
                     transition-colors hover:bg-s4-canvas hover:text-s4-ink"
              aria-label="<%= t("site4.nav.close_menu") %>">
        <%= render UI::IconComponent.new(:x, class: "size-4") %>
      </button>
    </div>
  <% end %>
</div>
